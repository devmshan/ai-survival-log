# Raw Layer Guide

- `raw/`는 불변 원본 자료 계층이다.
- 원본은 타입별 하위 폴더(`articles/`, `books/`, `journals/`, `videos/`, `podcasts/`)에 저장한다.
- 한 번 저장한 원본은 절대 직접 수정하지 않는다.
- 정리, 요약, 재작성, 태깅, 해설은 `wiki/`에서 한다.
- 예외는 구조 마이그레이션처럼 명시적으로 승인된 이동/이관 작업뿐이다.
- 위키 문서는 이 계층을 읽고 `wiki/sources/` 등으로 요약한다.
