# Output Layer Guide

- `output/`은 재생성 가능한 채널별 산출물 계층이다.
- 블로그 publish artifact는 `output/blog/`에 생성한다.
- 이 계층은 source of truth가 아니다. source of truth는 `wiki/`다.
- 최종 downstream 계약 경로는 계속 `ai-survival-log-site/content/posts/`다.
